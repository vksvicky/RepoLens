# Phase 6.7 Suppressions Implementation Plan

> **For agentic workers:** TDD; commit when user asks. Spec: [../specs/2026-08-06-phase-6.7-suppressions-design.md](../specs/2026-08-06-phase-6.7-suppressions-design.md)

**Goal:** Ignore file + disable comments stick across runs; fail-on/SARIF exclude suppressed; MD lists them; thin feedback CLI.

### Files

| Path | Role |
|------|------|
| `src/repolens/suppressions.py` | Load ignore, parse disables, match, split active/suppressed |
| `src/repolens/schema.py` | `SuppressedIssue` + `FindingReport.suppressedIssues` |
| `src/repolens/pipeline/run.py` | Apply after stamp_issue_ids |
| `src/repolens/report.py` | Markdown **Suppressed** section |
| `src/repolens/cli/commands_feedback.py` | `feedback down` / `list` |
| `tests/test_suppressions.py` | Unit + match rules |
| `docs/rules.md`, `faq.md`, `phases.md`, `CHANGELOG.md` | User docs |

### Tasks

1. TDD: ignore load + match + expiry + scanner vs disable
2. Wire pipeline + MD section
3. Feedback CLI appends ignore entries
4. Docs + phases

**Out:** Critical self-consistency; calibration feed from feedback.
