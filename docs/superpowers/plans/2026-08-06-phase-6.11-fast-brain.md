# Phase 6.11 Fast Brain Implementation Plan

> Spec: [../specs/2026-08-06-phase-6.11-fast-brain-whole-tree-heuristics.md](../specs/2026-08-06-phase-6.11-fast-brain-whole-tree-heuristics.md)

### Files

| Path | Role |
|------|------|
| `config.py` | `[fast_brain]` + `[general].max_files` |
| `inventory.py` | `max_files<=0` = uncapped |
| `heuristics/runner.py` | Parallel I/O workers |
| `pipeline/run.py` | Dual inventory; Fast Brain always; fingerprints on fast set |
| `pipeline/deep_exec.py` | Reuse Fast Brain heur result |
| `triage.py` | Optional heuristic hits |
| `schema.py` | `fastBrainFiles` / `llmPackFiles` provenance |
| tests + FAQ / atlas / phases | Exit criteria |

### Tasks

1. Config + dual inventory + provenance fields  
2. Always run parallel heuristics on fast inventory; deep reuses result  
3. Triage includes heuristic hits when configured  
4. Tests (>200 fixture finds outside LLM top-200) + docs  

**AST rule:** Fast Brain = regex/line/stat/hash only (no new AST heuristics).
